name: chatgpt-webview
base: core20 # the base snap is the execution environment for this snap
version: '0.1' # just for humans, typically '1.2+git' or '1.3.2'
summary: This is the standonle app for using chatgpt without opening your browser. # 79 char long summary
description: ChatGpt just launched its webview app for windows. This is the same unoffical build for a webview for chatgpt. 

grade: stable # must be 'stable' to release into candidate/stable channels
confinement: strict # use 'strict' once you have the right plugs and slots

parts:
  my-part:
    # See 'snapcraft plugins'
    plugin: nil
